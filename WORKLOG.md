# Generation Stability Worklog

## Baseline

Start time: 2026-09-22 23:00 Africa/Nairobi
Starting HEAD: 57177e66ce6b1d1e89c0e912b77212c7e138365b
Branch: codex/generation-stability
Working tree state: Pre-existing unrelated changes preserved: modified `.gitignore`; untracked `docs/architecture/generation-reliability-diagnosis-2026-09-22.md`. P0 added only `GENERATION_STABILITY_RUNBOOK.md`, `WORKLOG.md`, and `evidence/P0-baseline.md`.
Database topology: Not started for P0; no Docker services or local database touched.
Backend local config: Not inspected; no `.env` files read or changed.
Frontend local config: Not inspected; no `.env` files read or changed.

## Phase log

### P0 — Baseline
Status: PASS — accepted by Sol.
Findings: Current HEAD has persisted native realizations and teaching revision snapshots, but no single canonical teacher-facing preparation projection. Status API returns raw generation status/workflow stage and path realization rows. `lesson-context.ts` infers preparation state from stage substrings. Learn has its own output realization; Print admission continues to set `pack_id` and `output_id` to the preparation generation ID. See [P0 baseline](evidence/P0-baseline.md).
Commands: `git status --short --branch`; `git log -8 --oneline`; `git remote -v`; backend targeted pytest command and frontend targeted Vitest command recorded in evidence.
Tests: Backend: 117 passed, 2 warnings. Frontend: 9 passed.
Files changed: `GENERATION_STABILITY_RUNBOOK.md`, `WORKLOG.md`, `evidence/P0-baseline.md`.
Next: P0 accepted; proceed with the delegated P1 backend projection only.

### P1 — Canonical workspace truth
Status: PASS — accepted by Sol.
Findings: Added one canonical status projection on the existing path lesson status response. Preparation approval requires a persisted approved revision snapshot matching the approval pointer, status, plan revision, and plan identity. A newer valid pending Teaching Plan takes precedence over an older approved snapshot for current review state, while retaining the older approved identity separately. The route extracts teaching truth from persisted nested `page_document_v2`; worker stage remains progress/debug input. Structural `awaiting_review` is represented explicitly. Explicit failure outranks stale active-stage strings. Learn/Print realizations are projected independently, and a failed row cannot become ready because it retains an output pointer. Missing path rows are `not_created`; ambiguous legacy rows are surfaced without assigning identity. Preparation hash is deliberately not exposed as a content hash; hardening remains P2.
Commands: From `apps/textbook-agent/backend`: `uv run pytest tests/curriculum/test_workspace_projection.py tests/planning/test_path_routes.py::test_unprepared_lesson_status_is_explicit_over_http -q`; combined targeted suite `uv run pytest tests/curriculum/test_workspace_projection.py tests/planning/test_path_routes.py::test_unprepared_lesson_status_is_explicit_over_http tests/application/test_p03_realization_gates.py tests/curriculum/test_p02_shared_plan_gates.py tests/planning/test_native_retry_pre_worker.py tests/planning/test_phase02_queue_and_lease.py tests/reliability/test_p03_durable_budget_checkpoints.py tests/routes/test_d6c_learn_runtime_chain.py tests/routes/test_learn_adversarial.py -q`; `uv run ruff check src/curriculum/models.py src/curriculum/routes.py src/curriculum/workspace_projection.py tests/curriculum/test_workspace_projection.py tests/planning/test_path_routes.py`; `uv run ruff format --check src/curriculum/workspace_projection.py tests/curriculum/test_workspace_projection.py`.
Tests: Focused projection+route contract: 20 passed, 1 warning, 31.65 s. Combined targeted P1/P0 regression: 137 passed, 2 warnings, 93.86 s. Ruff checks passed. The existing warnings are Pydantic `schema` shadowing and `AgentRunResult.usage` deprecation.
Files changed: `apps/textbook-agent/backend/src/curriculum/models.py`, `apps/textbook-agent/backend/src/curriculum/routes.py`, new `apps/textbook-agent/backend/src/curriculum/workspace_projection.py`, new `apps/textbook-agent/backend/tests/curriculum/test_workspace_projection.py`, `apps/textbook-agent/backend/tests/planning/test_path_routes.py`, `GENERATION_STABILITY_RUNBOOK.md`, `WORKLOG.md`, and new `evidence/P1-canonical-state.md`. Pre-existing `.gitignore` and diagnosis document changes remain preserved.
Next: P1 accepted; proceed with bounded P2a backend content identity and consumer gate only.

### P2 — Teaching Plan approval
Status: P2a PASS and P2b PASS — accepted by Sol. P2 overall is PASS; P3 implementation is complete and awaiting Sol review.
Findings: Added a curriculum-owned content digest for all validated teacher-visible TeachingPlan fields, excluding only code-owned `teaching_plan_id`, `revision`, `preparation_hash`, and `approval_status`. Approval checks revision and optional expected digest and records `submitted` versus transitional `server_current_compat`. Both Learn and Print verify the persisted approved snapshot/digest before new work and pin identical content identity. Explicit pinned superseded snapshots remain consumable only when the digest verifies. Hashless historical approvals fail closed for new work and expose `reprepare`; existing output remains readable. Workspace projection and admission now agree that an omitted embedded revision is allowed only when ledger identity and digest verify. Shared-task/sourcebook identity follows pedagogical content independently of upstream preparation identity.
Commands: From `apps/textbook-agent/backend`, focused command: `uv run pytest tests/curriculum/test_p2_approval_content_identity.py tests/curriculum/test_p02_shared_plan_gates.py tests/curriculum/test_smart_lesson_contracts.py tests/curriculum/test_workspace_projection.py tests/planning/test_path_routes.py::test_unprepared_lesson_status_is_explicit_over_http tests/planning/test_phase02_queue_and_lease.py::test_approve_queues_without_executing -q`; regression command and exact Ruff check are recorded in [evidence/P2-approval-backend.md](evidence/P2-approval-backend.md).
Tests: Final focused run: 48 passed, 1 warning, 38.58 s. Final expanded backend regression including `test_workspace_projection.py`: 145 passed, 10 warnings, 156.63 s. Ruff passed on changed backend paths. Sol's `uv run python ../tools/agent/check_architecture.py --format text` reported `No architecture violations found` (exit 0).
Files changed: curriculum hash/revision/consumer logic; lesson review and sourcebook/shared-task hash consumers; Learn/Print hash adapters and handoffs; Print approval API/service/repository; P1 workspace projection alignment; focused P2a and P1 regression tests; [P2a evidence](evidence/P2-approval-backend.md).
Next: P2a accepted; proceed with bounded P2b frontend/API review and strict hash binding only.

P2b plan: Render the pedagogical Teaching Plan separately from review metadata on Units and Studio. Both active approval callers submit the displayed pending content hash; active backend approval rejects missing hashes. After approval, rehydrate and verify the persisted approved identity before rendering approval. A Print request is rejected with a typed conflict if a newer draft is pending, so it cannot auto-approve unseen bytes.
P2b evidence: [evidence/P2-review-frontend.md](evidence/P2-review-frontend.md).
P2b status: PASS — accepted by Sol. Final follow-up regression/check passed; P2 is complete and P3 design review is active.
P2b findings: Units and Studio now render TeachingPlan content separately from review status/revision. Both approval controls require a visible plan, pending status, verified pending hash, and matching plan/review/identity revisions. Both submit the exact loaded `expected_content_hash`, reload after approval, and require matching verified approved revision/hash before continuing. The review surface displays the verified pending/approved content hash separately from plan content. The active API rejects hashless approvals with typed `TEACHING_CONTENT_HASH_REQUIRED`. Print creation rejects a newer pending draft with typed `TEACHING_REVISION_PENDING_REVIEW` and does not mutate its pending review ledger.
P2b commands: Backend, from `apps/textbook-agent/backend`: `uv run pytest tests/curriculum/test_p2_approval_content_identity.py tests/curriculum/test_p02_shared_plan_gates.py tests/curriculum/test_smart_lesson_contracts.py tests/curriculum/test_workspace_projection.py tests/planning/test_path_routes.py tests/application/test_p03_realization_gates.py -q`; `uv run ruff check src/application/unit_lesson/realize_print_handoff.py src/print/http/v3_studio/router.py tests/application/test_p03_realization_gates.py tests/curriculum/test_p2_approval_content_identity.py`. Frontend, from `apps/textbook-agent/frontend`: `npm test -- src/lib/curriculum/lessons/teaching-plan-review.test.ts src/lib/curriculum/lessons/TeachingPlanReview.test.ts src/lib/api/v3.test.ts src/routes/studio/page.test.ts 'src/routes/units/[id]/lessons/[lessonId]/plan/page.test.ts'`; `npm run check`; `npm run build`.
P2b tests: Backend 69 passed, 1 existing Pydantic warning, 84.28 s; final focused frontend 55 passed across 5 files, including stale worker `ready` with valid current pending review; `npm run check` 0 errors and 5 existing warnings; final `npm run build` completed successfully with existing Svelte and optional-dependency warnings; Ruff passed. Exact commands/results are in [P2b evidence](evidence/P2-review-frontend.md).
Next: P2 accepted. P3 implementation is awaiting Sol's gate review.

### P3 — Print detachment
Status: PASS — accepted by Sol. P4 read-only design checkpoint active.
Findings: Print outputs have distinct Generation IDs and are linked to path-scoped realizations. Preparation approval state remains unchanged during admission, worker execution, failure, and retry. Output page state is initialized empty and receives only the pinned approved Teaching Plan plus allowlisted immutable semantic inputs. Worker status synchronization updates the owned realization and preserves stale/read-only states. Retry locks and refreshes the row before reserving a new output, supports verified superseded snapshots, and preserves prior failed artifacts. Studio uses the output ID as active generation, URL query, and status polling target; refresh reloads the verified approved plan and retry targets the detached output. Existing ready preparation-linked outputs stay readable; ambiguous/non-ready legacy records are read-only. Standalone Studio uses deterministic detached Generation output without `NativeRealizationModel`, whose schema requires a `PathLesson`; it requires explicit `native_whole_lesson` provenance, while missing Unit path/provenance fails closed.
Commands: From `apps/textbook-agent/backend`: `uv run pytest tests/application/test_p03_realization_gates.py -q` → 18 passed, 1 existing warning; `uv run pytest tests/planning/test_phase02_queue_and_lease.py tests/planning/test_native_retry_pre_worker.py tests/planning/test_native_retry_lease_fencing.py tests/planning/test_native_retry_durability.py tests/print_learn/test_p05_print_production_gates.py tests/planning/test_phase02_visual_pdf_routes.py tests/planning/test_path_routes.py tests/application/test_p03_realization_gates.py -q` → 161 passed, 7 existing warnings; `uv run ruff check src/application/unit_lesson/realize_print_handoff.py src/application/unit_lesson/realizations.py src/print/generation/whole_lesson/repository.py src/print/http/v3_studio/router.py src/print/http/v3_studio/dtos.py src/curriculum/routes.py tests/application/test_p03_realization_gates.py` → passed; `uv run python ../tools/agent/check_architecture.py --format text` → `No architecture violations found`.
Frontend: From `apps/textbook-agent/frontend`: `npm test -- src/routes/studio/page.test.ts` → 34 passed; `npm run check` → 0 errors, 5 existing Svelte warnings; `npm run build` → passed with existing Svelte warnings and optional-dependency notices. `git -c core.whitespace=cr-at-eol diff --check` passed.
Focused follow-ups: corrupted missing Unit provenance versus standalone fallback and standalone Studio output: 2 passed; corrupt existing output identity pointer: 1 passed; detached ready artifact ownership/linkage: included in final P3 suite. The ready-artifact regression verifies the realization links to the completed output, the owner-checked document reader resolves it, another user cannot read it, and the preparation approval remains approved.
Files changed: Print handoff/output lifecycle, worker repository status sync, curriculum retry route, Studio active-ID/query/status refresh, P3 backend/frontend regression coverage, and P3 evidence/runbook.
Next: P3 accepted. P4 implementation is complete and awaiting Sol's gate review.

### P4 — Learn/Print lifecycle symmetry
Status: PASS — accepted by Sol.
Findings: Learn admission commits a distinct queued output before provider work. A Learn-owned lifespan worker rechecks ownership, PathLesson, approval and pinned output metadata before running the existing producer under its output-scoped lease. Completion writes a ready document plus owner-linked EditableLesson against the output ID. Create does not auto-retry. Explicit retry accepts only `failed_recoverable` and conditionally increments revision/allocates one new output while preserving the prior output. Canonical error/status projection and serialized queued/running refresh are in place. Pathless Studio Learn returns `LEARN_PATH_LESSON_REQUIRED` with recovery guidance.
Commands: See exact commands/results in [P4 Learn lifecycle evidence](evidence/P4-learn-lifecycle.md). Focused P4 backend: 33 passed, 1 existing warning; final overlapping backend suite after retry CAS: 77 passed, 5 existing warnings. Frontend focused: 61 passed across 6 files. `pnpm run check`: 0 errors, 5 existing warnings; `pnpm run build`: passed. Ruff passed; architecture guard: `No architecture violations found`; scoped `git diff --check` passed.
Tests: Admission replay/202 route, real single-worker completion and editable link, Print/preparation isolation, corrupt foreign output protection, incomplete-ready rejection, explicit/concurrent retry with no orphan, stale nonretryable status, and queued-to-ready non-overlapping polling pass. Existing reliability tests cover competing output lease claims. Full two-worker PostgreSQL completion is deferred to P8 because Docker Desktop's daemon is unavailable (`docker version` cannot connect to `//./pipe/dockerDesktopLinuxEngine`).
Files changed: Learn handoff/worker/lifespan, retry/status/error projection, Unit and Studio Learn routes, serialized frontend polling, focused tests, P4 evidence/runbook, and this worklog. Existing `.gitignore` and untracked diagnosis note are preserved.
Next: P4 accepted. P5 correction-boundary implementation is complete and awaiting Sol gate review.

### P5 — Unified repair ownership
Status: PASS — accepted by Sol.
Findings: `LLMAuthoringProvider` sends generic JSON output through one `run_structured_agent` call with pydantic-ai output retry disabled and V3 `RetryPolicy(max_attempts=1)`. The engine classifies malformed structured output as same-work-item correction input, transport/timeouts as an ambiguous consumed slot, and programming/provider/auth/config failures as terminal; budget reservation and actual dispatch counts are explicitly tested. Document quality, composer schema/allowlist/coverage semantics, and interaction envelope checks run inside engine validation before ready checkpoint commit. Generic missing-provider and transport failures do not fall back. The Print production caller explicitly selects budgeted `heuristic_only` mode when no LLM composer is configured, and the persisted selection trace exposes its degraded reason. Print teaching/form retain separate bounded planners with typed failure-class coverage.
Commands: From `apps/textbook-agent/backend`: expanded focused regression including integrated offline authoring, P08 integration, and P04 worker: `uv run pytest tests/authoring_correction/test_a02_shared_authoring_engine.py tests/generation/test_writer_repair.py tests/authoring_correction/test_a04_learn_authoring.py tests/learn/authoring/test_builder_document_v2.py tests/print_learn/test_composition_bridge.py tests/reliability/test_p03_durable_budget_checkpoints.py tests/planning/test_phase02_failure_classification.py tests/authoring_correction/test_a00_arbitrary_answers.py tests/authoring_correction/test_a06_integrated_offline.py tests/print_learn/test_p08_integration_gates.py tests/application/test_p04_learn_worker.py -q`; architecture: `uv run python ../tools/agent/check_architecture.py --format text`; scoped Ruff command: see [P5 implementation evidence](evidence/P5-correction-boundary-implementation.md). Scoped diff check: from repository root, `git -c core.whitespace=cr-at-eol diff --check`.
Tests: Expanded regression: 72 passed, 5 existing warnings, 44.40 s. The actual `run_llm` boundary test proves malformed output/correction has two dispatches and two consumed dispatched slots; 429 and timeout each have one dispatch and one consumed ambiguous slot; a programming `AssertionError` consumes one dispatched slot and terminates as `PROVIDER_FAILURE` without repair. The P08 Print cases pass and verify persisted degraded-composition provenance. Ruff passed; architecture reported `No architecture violations found`; scoped diff check passed. Sol accepted P5 PASS.
Files changed: Authoring provider/engine/error types, structured helper retry-policy override, document writer/composer validators and fallback policy, explicit Print heuristic-only caller mode, Learn authoring/interaction validators, Print error classification, focused/integrated backend tests, and this runbook/worklog/evidence. Earlier phase changes and pre-existing `.gitignore`/diagnosis edits remain preserved.
Next: P5 accepted; P6 subsequently accepted. P7 read-only failure/concurrency audit is active.

### P6 — Frontend consumes backend truth
Status: PASS — accepted by Sol.
Task: Use canonical workspace state for Unit lesson preparation, Learn, and Print. Keep Studio generation-detail state for editor/output routes without PathLesson lookup.
Evidence: [P6 inventory/design](evidence/P6-frontend-truth-inventory.md) and [P6 implementation evidence](evidence/P6-frontend-canonical-state.md).
Next: P6 accepted. P7 read-only audit is active; wait for Sol review before implementation.

### P6 — Frontend truth
Status: PASS — accepted by Sol.
Findings: Unit preparation and Learn/Print lifecycle now use canonical workspace projection exclusively for lifecycle state/identity/retry. Create is gated on fresh verified approval, failed status refresh is surfaced, and document-preview failure does not offer realization retry. Studio product routes were not changed.
Commands: From `apps/textbook-agent/frontend`: `npm test -- src/lib/curriculum/lessons/lesson-context.test.ts src/routes/units/[id]/page.test.ts 'src/routes/units/[id]/lessons/[lessonId]/plan/page.test.ts' 'src/routes/units/[id]/lessons/[lessonId]/learn/page.test.ts' 'src/routes/units/[id]/lessons/[lessonId]/print/page.test.ts' src/routes/studio/page.test.ts`; `npm run check`; `npm run build`.
Tests: 73 passed across 6 files. Svelte check: 0 errors, 5 pre-existing warnings in unrelated editor files. Build passed with existing warnings and optional dependency notices. Scoped diff check passed. A queued Print regression confirms queued guidance offers refresh only and no Retry action.
Files changed: Unit workspace DTO and canonical lesson-context helper, Unit dashboard, lesson layout, Plan/Learn/Print routes, canonical helper/route tests, issue-panel retry guard, and P6 evidence/runbook.
Next: P6 accepted; P7 read-only matrix/design awaits Sol review.

### P7 — Failure injection
Status: PASS — accepted by Sol. P7 is complete.
Findings: Added persisted Teaching Plan repair, actual invalid/5xx/auth provider dispatch accounting, independent Learn/Print state table, expired Learn worker parking with ready Print sibling preservation, five corrupt/foreign output cases, detached Print export-failure isolation, typed error projection-to-UI, and poll stop/restart proofs. Fixed the Print page to prefer canonical realization failure details over a simultaneous preview-fetch error. Full results and limits are in [P7 failure-injection evidence](evidence/P7-failure-injection.md); the original audit is retained in [P7 matrix](evidence/P7-failure-matrix.md).
Commands: Backend full targeted regression: 217 passed, 9 warnings in 115.34s. Frontend focused suite: 84 passed across 7 files. `npm run check`: 0 errors, 5 existing warnings. `npm run build`: passed. Scoped P7 Ruff passed, architecture guard passed, and `git -c core.whitespace=cr-at-eol diff --check` passed. Exact commands are recorded in the evidence file.
Tests: `tests/planning/test_pre_worker_failure_sync.py`, `tests/authoring_correction/test_a02_shared_authoring_engine.py`, `tests/curriculum/test_workspace_projection.py`, `tests/application/test_p04_learn_worker.py`, `tests/application/test_p03_realization_gates.py`, lesson-context/poll tests, and Plan/Print route tests are included in the expanded commands.
Files changed: P7 targeted tests, Print failed-state error presentation, evidence/runbook/worklog. No P7 backend product change.
Limits: Browser navigation and PostgreSQL competing-worker completion remain for P8. The Print failure test injects at repository failure persistence rather than invoking the renderer; worker interruption is simulated through persisted expired-lease state rather than OS process termination. Broad Ruff finds issues in three unchanged backend source files; scoped P7 lint passes.
Next: P8 local proof preparation and walkthrough are active.

### P8 — Local live proof
Status: PASS — live browser/DB proof complete; handed to Sol for final acceptance.
Findings: Docker PostgreSQL `textbook-agent-db-1` and native backend/frontend supported a real authenticated walkthrough. Teaching Plan revision 1/hash `b47708dba7f066ea62fec3483f291ea79b702b788eaf4148bcab624a1d91d5a2` was visibly reviewed/approved. The first two Learn outputs expired across controlled process restarts and reconciliation parked them as `failed_recoverable`. This exposed a missing `session.commit()` after successful work in the disposable-session worker. Added success commit plus guarded escaped-finalization handling. A third explicit retry produced ready output `learn-out-6603dafebcb14015` and owner-linked editable lesson `84c64909-ff6c-468d-85ff-2d6080247271`; the source approval and ready Print output `cf88fb9a-2ab0-49d6-8514-74afc6f4e0bf` remained unchanged. Authenticated Learn and Print both rendered ready, survived refresh, and the Learn preview interaction returned correct feedback without saving attempts.
IDs: Unit `c85de318-1c08-40aa-9237-3432e377732f`; lesson `ad945ebb-c754-48b2-8fbe-a9e9cf5c3264`; path version `7cfbde4e-b360-4da5-b3f9-db1a85109850`; preparation generation `c88bbe41-4780-45ff-951c-a91eb98bf6ae`.
Commands/results: `uv run pytest tests/application/test_p04_learn_worker.py -q` → 16 passed, 1 existing warning; Plan route test → 6 passed; frontend canonical suite → 85 passed; `npm run check` → 0 errors/5 existing warnings; `npm run build` passed. Ruff, architecture guard, and CRLF-aware diff check passed. Exact live details are in [P8 local proof evidence](evidence/P8-local-proof-preparation.md).
Files changed: Plan stale-stage recovery, Learn worker success/finalization, focused regressions, P8 evidence, runbook/worklog. Existing `.gitignore` and diagnosis document remain untouched.
Next: P8 evidence is complete; P9 and P10 are underway.

### P9 — Cleanup
Status: PASS — cleanup, compatibility, regression, and artifact/security audit complete; handed to Sol for final acceptance.
Findings: Removed only five generated current-run `.playwright-cli` artifacts. Preserved older ignored artifacts, modified user `.gitignore`, and untracked diagnosis note. Canonical Unit vs Studio state-source compatibility was audited. An expanded backend run first reported 321 pass/2 fail: one resume fixture lacked persisted revision-bound semantic artifacts, and one short timing assertion raced. The fixture was corrected to seed valid persisted artifacts (no product behavior change); D01 passed both isolated and aggregate reruns. The final expanded aggregate passed 323 tests. Details: [P9 cleanup and compatibility audit](evidence/P9-cleanup-compatibility-audit.md).
Commands: Architecture guard `uv run python ../tools/agent/check_architecture.py --format text` → `No architecture violations found`; scoped Ruff for all changed backend Python paths passed; CRLF-aware diff check passed; changed/untracked path scan found no env files, logs, DB files, or browser profiles. Frontend suite 85 passed; `npm run check` 0 errors/5 existing warnings; `npm run build` passed.
Tests: Final backend expanded regression across 21 files: 323 passed, 14 existing warnings, 197.77s. P04 worker fresh-session/finalization regression: 16 passed, 1 warning, 59.50s. Frontend canonical regression: 85 passed across 7 files. Full commands are in P9 evidence.
Files changed: task code/tests, P8/P9 evidence, runbook/worklog, final report. No unrelated user files are staged.
Next: P9 gates are complete; perform P10 task-only staging, final report, commit, and push.

### P10 — Push/report
Status: PASS — task-only commit pushed and remote SHA verified.
Final task commit: `b9f7abb141404ee67e17205aa237ea570155c40b`.
Remote branch: `origin/codex/generation-stability`; `git ls-remote` matched the local task commit.
Final report: [FINAL_REPORT.md](FINAL_REPORT.md).
Final notes: 94 task-owned files committed. Modified `.gitignore` and untracked `docs/architecture/generation-reliability-diagnosis-2026-09-22.md` remain preserved and uncommitted. The private temp Uvicorn log remains outside the repository. No env, database, browser-profile, or generated CLI files were staged.
