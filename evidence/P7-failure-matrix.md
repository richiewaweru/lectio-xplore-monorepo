# P7 — Failure injection and concurrency coverage matrix

Status: Audit and approved P7 test tranche implemented; awaiting Sol's gate review. See [P7 failure injection evidence](P7-failure-injection.md) for exact commands/results. No P8 work has started.

## Audit scope

Compared `C:\Users\richi\Downloads\lectio_generation_stability_pack_read_20260922\04_TEST_PLAN.md` with the accepted P0–P6 evidence and the current named tests in the repository. The invariant matrix below preserves the initial audit and proposed seams; the approved P7 tranche has now added the targeted proofs detailed in the implementation evidence. Browser navigation and PostgreSQL competing-worker completion remain P8 work.

## P7 implementation result

The approved deterministic tranche now covers persisted Teaching Plan repair/exhaustion, actual dispatch counts for repeated invalid output/5xx/auth, independent Learn/Print states, expired Learn work parked beside a ready Print sibling, five corrupt/foreign output identity variants, detached Print export-failure isolation, typed error projection-to-UI, and poll stop/restart. Results and limits are in [P7 failure injection evidence](P7-failure-injection.md). One real UI defect was found and fixed: a failed Print preview request could obscure the canonical realization failure message. No owner-isolation or worker-state product defect was found by this tranche.

## Invariant matrix

| Test-plan invariant | Existing accepted evidence/tests | Coverage | Smallest deterministic P7 seam |
|---|---|---|---|
| Stale revision/hash, same-revision content replacement, missing approval pointer/ledger row, persisted digest mismatch, and prior snapshot readability | `tests/curriculum/test_p2_approval_content_identity.py`: `test_approval_rejects_stale_revision_and_displayed_content_hash`, `test_approval_rejects_same_revision_replacement_and_persists_snapshot_digest`, `test_consumer_rejects_missing_or_mismatched_persisted_approval_hash`, `test_consumer_rejects_missing_approval_pointer_or_ledger_row`, `test_older_approved_snapshot_remains_readable_by_explicit_revision_after_new_approval`, `test_active_lesson_approval_rejects_missing_content_hash`; `tests/curriculum/test_workspace_projection.py` approval/hash projection cases; Plan frontend tests verify visible content and exact submitted hash. | **Covered** for backend admission and displayed-hash submission. **Partial** for the browser-visible response to a typed stale-hash conflict and re-review recovery. | Add a Plan route test whose approval POST returns the API's typed hash conflict after a same-revision replacement; assert the stale plan is not presented as approved and the teacher is prompted to reload/review. No new backend seam needed. |
| Planning and Teaching Plan malformed output, semantic validation, bounded repair success/exhaustion, exact attempt count/error/retryability, and no false approval | `tests/planning/test_contract_hardening.py` repair/validation tests; `tests/generation/test_native_all_forms_e2e.py::test_invalid_json_then_valid_repair`; `tests/generation/test_writer_repair.py`; `tests/planning/test_native_retry_pre_worker.py` invalid teaching/item recovery and retry tests; `tests/planning/test_phase02_failure_classification.py`; P2 approval ledger tests prove invalid/legacy approval is not verified. | **Partial.** Invalid JSON, teaching semantic repair, retry classification, and repair exhaustion exist at separate layers. Accepted evidence does not show one concise persisted preparation-run assertion for exact stage attempt counts and final typed projection across structural-planner and Teaching Plan failures. | Parameterize one deterministic pre-worker fake-model case per structural planner and Teaching Plan: success on bounded repair and exhausted invalid response. Assert provider calls, persisted checkpoint/attempt count, typed error fields, retryability/recovery action, and no approval. Reuse the existing failure-sync fixture; do not add a provider dependency. |
| Authoring work-item defects, one budget, typed transport/auth/programming errors, and no repair after non-content failures | P5 `test_a02_shared_authoring_engine.py` includes actual `run_llm` dispatch/ledger accounting, malformed structured-output repair, and terminal provider failure; `tests/planning/test_phase02_failure_classification.py`; `tests/reliability/test_p03_durable_budget_checkpoints.py` includes budget, crash-before/after-commit, selective resume, document quality repair/exhaustion, and fallback policy; P5 evidence records the 72-test expanded suite and exact 1-dispatch/2-dispatch cases. | **Mostly covered.** Current actual-boundary accounting explicitly exercises malformed output, HTTP 429, timeout, and programming exception. Provider 5xx/auth classification and repeated identical invalid payload are not established as one exact-dispatch assertion in the accepted evidence. | Extend the existing monkeypatched `run_llm` boundary test with repeated identical invalid output, one 5xx, and one auth/config exception. Assert every dispatch is accounted once, repair only applies to model-correctable output, and no repeated dispatch exceeds the declared work-item budget. |
| Learn/Print isolation across the required combinations; approval remains intact under either path failure | `tests/application/test_p03_realization_gates.py` covers dual-output idempotence, Print retry preserving Learn/plan, detached-output/failure retry, stale completion, and ready artifact link; `tests/application/test_p04_learn_worker.py` covers Learn-only completion, failure/retry, and sibling/output isolation; `tests/print_learn/test_p08_integration_gates.py` covers uninterrupted dual path, no plan swap, and teacher edit/sibling isolation; workspace projection tests cover path filtering and approved state surviving Print failure. | **Partial.** Independent lifecycle and representative failure combinations are covered. The pack's full Learn/Print truth table (each ready/running/recoverable/terminal/not-created combination) is not parameterized end-to-end. | Add a parameterized pure `project_lesson_workspace` truth table for representative independent state pairs and verify each path's identity/error remains path-local. Retain existing integration tests for real admission/worker behavior; avoid duplicating those fixtures for every pair. |
| Duplicate Create, retry ownership, idempotence, no orphan output, and source/sibling immutability | P3 tests cover detached Print duplicate admission, output identity mismatch, concurrent Print retry winner, retry of a superseded pinned snapshot, and unchanged preparation; P4 tests cover duplicate Learn Create, Create replay leaves a recoverable failure parked, explicit and concurrent retry with exactly one output/no orphan, foreign output, and ready completeness. | **Covered** for sequential Create replay and concurrent retry winner on the configured SQLite fixture. **Partial** for concurrent duplicate Create against the production DB and cross-path concurrent Create. | Add a bounded DB-level concurrent duplicate Create test if it can use the existing async SQLite harness; assert one realization/output identity and no orphan. Keep PostgreSQL row-lock/CAS behavior as the P8 live proof because P4 explicitly deferred that proof. |
| Worker lease expiry, competing claims, stale fencing, crash/restart, completed sibling preservation, and failed work parked until explicit retry | `tests/planning/test_phase02_queue_and_lease.py` covers two workers claiming once, recoverable work parked, stale contention winner, fresh heartbeat, stale write rejection, and wrong-token heartbeat; `tests/reliability/test_p03_durable_budget_checkpoints.py::test_g09_g11_crash_injection_before_after_commit`, `test_g10_selective_recovery_skips_ready_sibling`, and `test_g13_learn_fencing_expired_and_cancel`; P4 worker tests cover durable queue, single-worker completion, corrupt foreign output, and explicit retry. | **Partial.** Lease/checkpoint components are tested. There is no accepted end-to-end Learn worker test that abandons a claimed run after model dispatch, expires its persisted lease, restarts the worker, and proves the ready sibling is not re-dispatched while failed work remains parked. SQLite also does not establish Postgres lock completion. | Use the existing queued Learn worker fixture and monkeypatch the writer/provider to pause/raise at a known checkpoint after dispatch. Persist an expired lease, invoke existing reconciliation, then run the worker again. Assert dispatch counts, checkpoint reuse, terminal/recoverable state, and sibling preservation. Use a Docker PostgreSQL service for the two-worker completion/race portion in P8. |
| Typed failure class and retry action survive persistence → workspace API → frontend | `tests/planning/test_phase02_failure_classification.py`; `tests/curriculum/test_workspace_projection.py::test_preparation_failure_projects_underlying_typed_error_details` and artifact projection tests; P4 Learn worker failure tests; P6 `lesson-context.test.ts` tests canonical failure/retry gates, and Unit dashboard tests surface failed refresh. | **Partial.** Backend error typing and UI retryability are tested separately; one route-to-UI assertion for representative recoverable vs terminal typed failure is not recorded. | Add focused projection-to-helper contract fixtures for `MODEL_OUTPUT_INVALID`, transport timeout, terminal provider/programming failure, and stale/hash-mismatch recovery; assert exact retryability/action at the teacher-facing UI boundary. No app-level model call is required. |
| Canonical frontend states, poll stop/restart rules, refresh reconstruction, and no infinite polling | P6 helper tests cover all six preparation states, contradictory debug fields, path isolation, and explicit retryability; dashboard covers failed status refresh; P4 `serialized-poll.test.ts` covers queued→ready and no overlapping refresh; Plan/Learn/Print tests cover fresh-approval and relevant failure/preview cases; P6 Studio tests preserve generation-detail behavior. | **Partial.** Canonical mapping, a refresh error, one queued→ready poll, and Studio preservation are covered. Not every active/stop state has an explicit timer assertion; there is no browser refresh test that reconstructs each canonical state from a fresh backend response. | Extend the serialized-poll fake-timer table for awaiting teacher approval, ready, recoverable, terminal, and explicit retry restart. Add one refresh/reload fixture per core state on a path page, with no worker-stage fields in the fixture. Keep browser navigation E2E for P8. |
| Visual/Print rendering/export failure is truthful, does not damage Learn or approval, and cannot claim ready without required output | `tests/planning/test_native_retry_pre_worker.py::test_r06_visual_failure_not_owned_by_retry_native`; P3 ready output document/open-path linkage and detached output tests; P5/P08 composer fallback and degradation provenance tests; P6 Print preview-fetch failure keeps ready state and suppresses realization Retry. | **Partial.** Preview retrieval failure, visual classification, and composer degradation are covered. An injected Print render/PDF export failure persisted against the detached output, with source approval and Learn sibling asserted unchanged, is not shown in accepted P3–P6 evidence. | Patch the Print renderer/export boundary in the existing P3 realization fixture to fail after admission. Assert typed output error, realization failed/not-ready, source approval and Learn row unchanged, and retry targets only Print. Add a separate missing-required-document assertion if the read route accepts an output pointer without a document. |
| Foreign-owner/corrupt output and path identity never mutate or expose another owner's artifact | P3 output pointer owner/plan metadata validation, ready owner-checked read/open route, corrupt output ID conflict, and missing Unit provenance failure; P4 `test_p04_worker_parks_corrupt_foreign_output_without_mutating_it`; P1 legacy ambiguity projection; P3 ambiguous legacy records remain read-only. | **Covered** for Print admission/read and Learn worker non-mutation on the tested corrupt foreign pointer; **partial** for a cross-user status/open-route matrix over both Learn and Print and every mismatch field. | Add a shared parameterized ownership contract test for foreign user, wrong path, source preparation, Teaching Plan ID/revision/hash, and realization ID; verify GET/open never returns content and worker/error handling never mutates foreign output. Reuse the existing route fixture and row-mutation helper. |
| Local browser happy path and controlled failure/retry from the teacher's perspective | P6 route-level component tests and P2 review/approval UI tests; backend P08 fake-provider integration tests establish dual generation behavior. P4 evidence says PostgreSQL two-worker completion deferred to P8 because Docker daemon was unavailable at that time. | **Not yet demonstrated end-to-end.** Component and backend integration tests are not a browser-driven local application proof. | Reserve for P8: locally run the app against Docker PostgreSQL, use a deterministic test failure hook/fake provider, create/review/approve/realize both paths, refresh/open outputs, then retry one controlled failure while proving the healthy sibling and approval remain unchanged. |

## Proposed bounded P7 test tranche

1. Add the missing persistent failure-injection cases for planner/Teaching Plan repair, actual authoring dispatch classes, and Print rendering failure using existing fake providers and integration fixtures.
2. Add compact table-driven truth/error/poll assertions rather than duplicating full model-generation tests for every state pair.
3. Add one end-to-end Learn worker interruption/recovery proof using persisted expiry and existing fencing/reconciliation APIs; keep the true PostgreSQL competing-worker completion proof for P8.
4. Add route-level cross-owner/corrupt output checks only for unproven path/mismatch dimensions, retaining the existing strong owner and corruption tests.

No architecture blocker surfaced in this audit. The known SQLite-vs-PostgreSQL boundary is explicit in accepted P4/P8 evidence and is not treated as a substitute for the planned P8 Docker/PostgreSQL proof.
