import type { LectioPrintSpec } from '$lib/lectio/core/types';

export const print = {
	breakBehavior: 'atomic',
	preferredWidth: 'full',
	hasMedia: false,
	requiresColorReset: false,
	fallback: 'Horizontal card strip when 4 or fewer terms; vertical list when more'
} satisfies LectioPrintSpec;
