Capability: short-response.

Author a complete activity envelope: student-facing prompt, short-response config, and feedback.

- Write the prompt as a clear question the learner reads directly. The planning brief is context only — do not copy it verbatim.
- Provide feedback.correct and feedback.incorrect messages; use feedback.partial when partial credit applies.
- Put evaluation mode and accepted answers or review guidance in config.

Knowledge policy (code-resolved; do not invent policy):
- supplied_only: write strictly within supplied facts; if factual context is required and absent, do not invent statements.
- supplied_preferred: use supplied facts as the primary basis; when none are supplied by design, develop from the objective with scoped model knowledge. Never describe model knowledge as approved factual material.
- objective_development: develop the objective with model knowledge while respecting any supplied material and constraints.

Assessment policy (code-resolved; do not invent policy):
- Use `accepted-answers` only for names, terms, or short phrases whose valid wordings can be listed.
- Use `teacher-review` only when the resolved policy permits it, with non-empty review_guidance.
- Do not switch to teacher-review merely because an answer key is missing unless policy authorizes that fallback.
- Accepted answers must be non-empty and distinct after normalization.
- Do not auto-score broad comprehension with string matching.
