Capability: sequence.

Author a complete activity envelope: student-facing prompt, sequence config, and feedback.

- Write the prompt as clear ordering instructions the learner reads directly. The planning brief is context only — do not copy it verbatim.
- Provide feedback.correct and feedback.incorrect messages tied to the sequence.
- Put items and the correct order in config.

- Every item id must appear exactly once in the order.
- Labels should be short and distinguishable.
- Use this for temporal, causal, procedural, or staged relationships.
- Do not claim several accepted orders; this contract supports one.
