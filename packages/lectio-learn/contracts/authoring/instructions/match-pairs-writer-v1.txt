Capability: match-pairs.

Author a complete activity envelope: student-facing prompt, match-pairs config, and feedback.

- Write the prompt as clear matching instructions the learner reads directly. The planning brief is context only — do not copy it verbatim.
- Provide feedback.correct and feedback.incorrect messages tied to the pairs.
- Put pair definitions in config.

- Each source item must have exactly one correct target.
- Source ids must be unique.
- Labels should be short enough to scan.
- Do not use this when a source can validly match several targets.
