# Remaining stability run — 2026-08-12

Starting SHA: `26741d0866cb0910504f4092c3f4a3eb19f32c4c`  
Working tree: dirty (preserved; not reset)  
Ports: frontend 5173 / backend 8000 only

## Checklist

- [x] Read authority files; record starting SHA/dirty tree without resetting it
- [x] R1 model-backed QC-before-upload/completion
- [x] R1 regressions
- [x] R2 evidence capture and verifier
- [x] R2 regressions
- [x] R2A exact-generation form-timeout proof control
- [x] R3 deterministic confidence gate
- [x] LIVE PROVIDER CALLS AUTHORIZED BY USER: **yes** (targeted proofs only)
- [ ] R4 targeted proofs A–E — **in progress** (login blocker on A–D)
- [ ] R5 official lessons 1–4 — blocked (out of scope)
- [ ] R6 final matrix summary — blocked (out of scope)

## R3 results

Backend focused native gate: **310 passed**, 1 existing Pydantic warning.

Frontend focused vitest: **8 files / 85 tests passed**.  
`svelte-check`: **0 errors / 0 warnings**.

Architecture: no violations.  
Prompt verifier: `prompt_checksums=ok`, v2 SHA `2ccc4c7b...`.

`validate_repo.py --scope all`: **PASS** (exit 0, 610s)

- backend-ruff: PASS (`src/` `tests/`)
- backend-pytest: **1152 passed**, 1 warning, 330.30s
- frontend-check: PASS
- frontend-build: PASS
- tooling-pytest: **8 passed**

`ruff check src tests scripts` reports 6 pre-existing findings in unrelated scripts (`diagnose_v3_image_pipeline.py`, `patch01_native_entry_smoke.py`, `run_simplified_path_acceptance.py`, `run_whole_lesson_proof.py`). None are in R1–R2A files. `validate_repo` ruff scope (`src/` `tests/`) is clean.

Health: `http://127.0.0.1:8000/health` → 200; `http://localhost:5173/units` → 200.

## Live authorization

```
LIVE PROVIDER CALLS AUTHORIZED BY USER: no
timestamp: 2026-08-12T13:16:00Z
authorized scope: none
```

R4–R6 are blocked. No paid provider call, live retry, generation create, or DB progression was made. Reply in this chat with:

```
LIVE PROVIDER CALLS AUTHORIZED BY USER: yes
authorized scope: targeted proofs only | targeted proofs plus final matrix
```

to continue.

## R1

Topology recovery now keeps final PNG bytes, runs deterministic integrity QC as preflight, then model-backed visual QC on those exact bytes before `store_image` / `apply_visual_completion`. Dispatch injects the QC adapter for the production recover path. Flag/reject/error/malformed verdicts do not upload or complete.

Focused result: 64 passed (visual topology/recovery/dispatch/QC/renderer/compositor). Ruff clean.

## R2

Capture writes protocol artifacts 00–39 plus telemetry/legacy/native-identity extras. Browser PNG/PDFs are copied from explicit paths only. Verifier exit 0/2/1 as specified.

## R2A

`planning_forms` exact-generation one-shot `TimeoutError` hook. Production-like `APP_ENV` refuses activation. Writer-block injection unchanged.

## Live authorization

```
LIVE PROVIDER CALLS AUTHORIZED BY USER: no
timestamp: 2026-08-12
authorized scope: none
```

R4–R6 are blocked until the user explicitly authorizes paid/live provider calls.

## R4 live authorization (targeted proofs A–E)

```
LIVE PROVIDER CALLS AUTHORIZED BY USER: yes
timestamp: 2026-08-14T12:49:00Z
authorized scope: targeted proofs only
```

Infrastructure: Docker Postgres (`textbookagent-db-1` on `127.0.0.1:5432`), backend `http://127.0.0.1:8000`, frontend `http://127.0.0.1:5173`. R5 official matrix and R6 STABLE decision remain out of scope.

## R4 infrastructure (2026-08-14)

| Check | Result |
| --- | --- |
| Docker daemon | running |
| Postgres container | `textbookagent-db-1` healthy, `0.0.0.0:5432->5432` |
| Backend health | `GET /health` → 200, version `1.0.0-beta.1`, pipeline `shell-pipeline-native-lectio` |
| Backend postgres dep | `GET /health/ready` → postgres `ok` (Docker, not Railway) |
| Frontend health | `GET /units` → 200 (Vite on `127.0.0.1:5173`) |
| Browser session | Playwright headed opened; redirected to `/login` (Google sign-in required) |
| Login pause artifact | `docs/evidence/whole-lesson-runs/targeted-login-pause/00-login-pause.png` |

**User action required:** Sign in at `http://localhost:5173/units` (Simple Browser or Playwright headed window). No JWT minting or proof-runner bypass. After login lands on `/units`, re-run proofs A–D in order.

## R4 targeted proofs A–E (2026-08-14)

| Proof | Verdict | Generation / unit | Evidence path | Notes |
| --- | --- | --- | --- | --- |
| A — form timeout + checkpoint retry | **FAIL** | — | — | Blocked at login. R2A hook not exercised; no `targeted-a-form-timeout` capture. |
| B — Home/New native-only | **FAIL** | — | — | Blocked at login. No new generation created through `/units`. |
| C — ready auto-navigation | **FAIL** | — | — | Blocked at login. Depends on B or D reaching `ready`. |
| D — real visual + visual-only recovery | **FAIL** | — | — | Blocked at login. No Water Cycle (or equivalent) visual run; no PDFs/hashes. |
| E — worker reclaim | **PASS** | `5c254377-4d7f-40bd-a599-d9a9dd3e0bab` | `docs/evidence/whole-lesson-runs/stability-20260809/RECOVERY_RUN_5C254377.md` | Reused prior live evidence: old owner `native-17cb835acf80` token 1 → kill → lease expiry → new owner `native-1e907ab8e4f0` token 2 reclaim at item checkpoint; zero stale-owner writes after reclaim. |

Prior diagnostic runs (`5c1407d3…`, `ea292446…`) are explicitly excluded from targeted proof A/B/D per their recovery reports.

**Overall R4 verdict:** **NOT COMPLETE** — 1/5 proofs passed (E only). Proofs A–D require authenticated UI session. Verdict remains **NOT YET STABLE**. R5 matrix not started (per plan stop gate).
