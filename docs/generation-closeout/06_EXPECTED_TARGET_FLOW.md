# Expected Target Flow

```text
                    UNIT / PATH LESSON
                           │
                           ▼
                    TEACHING PLAN
            teaching moves + learner actions
                           │
                    teacher approves
                           │
                    explicit path choice
              ┌────────────┴────────────┐
              │                         │
            PRINT                     LEARN
              │                         │
      document.composer        document.composer
              │                         │
       document.writer          document.writer
              │                         │
     Print action policy       Learn action policy
              │                         │
   Print task treatment        retained interaction
              │                         │
   LectioDocument v2          LearnDocument v2
              │                         │
   Print viewer/editor        Learn editor/tabs
              │                         │
         pagination               runtime/eval
              │                         │
             PDF                       WEB
```

Mutable reasoning comes from registered Markdown specs. Legal vocabularies/mappings come from policy files. Structural validity remains in code.
