# Lectio V3 Retirement Casa Pack

This pack is designed for an autonomous Casa Grok implementation run.

Start at `00_START_HERE.md`.

The pack deliberately separates:
- **current code living in old namespaces** -> move/rename;
- **retired V3 execution architecture** -> delete;
- **next-generation shared writer/continuity work** -> explicitly out of scope.

The migration is not complete until the native end-to-end workflow passes after the old V3 execution code has been physically removed.
