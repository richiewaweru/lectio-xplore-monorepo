# P02 — Stage contracts and safe admission
Required gates: G05 G06 G07 G08

## Implement
Inspect existing DB/state machinery; create minimal additive stage/run/item/attempt/event representation or extend equivalent existing records. Define domain-owned stage graphs. Allocate stable identities before calls. Implement scoped idempotency and concurrent duplicate protection for generation, save, publication/export operations. Preserve artifact schemas and approval behavior. Provide migration/rollback plan and existing-record compatibility.

## Gate conditions
Test wrong-owner access, conflicting key payloads, repeated save response, concurrent admissions and immutable teaching revision. No UI-driven implicit production or shared sibling mutable state.

## Required phase output
Write tracking/P02-REPORT.md with baseline/implementation SHA, actual files changed, intent, migrations, commands with cwd/start/end/exit, gate-specific assertions and evidence paths, failures/fixes and unresolved issues. Update tracking/STATE.json only from observed evidence. Use PASS, FAIL, BLOCKED or NOT_RUN. A reported PASS must correspond to the committed tested code; docs-only evidence stamps may name the prior tested implementation commit explicitly.

## Agent execution instruction
Read this phase, proposal, inspection notes and relevant strict-gate rows. Trace existing implementation before editing. Preserve unrelated work. Make the smallest complete change satisfying behavior. Run required checks; fix regressions and repeat affected checks. Do not advance with unmet dependency gates or weaken acceptance rules. If blocked externally, continue independent authorized work and preserve a restart note; leave final readiness blocked. No automatic merge.
