# P03 — Durable work and bounded recovery
Required gates: G09 G10 G11 G12 G13 G14 G15

## Implement
Checkpoint composition and IDs, nodes, interactions, paper treatments/media and assembly/export; preserve Print safeguards. Add durable Learn run execution through existing suitable infrastructure. Unify transport/schema/quality repair call budget across nested engine calls. Add leases/fencing, deadlines, cancellation, concurrency caps and explicit version checks. Route provider errors to policies. Keep media failures local and fallback visible.

## Gate conditions
Inject crashes before call, after call before commit, and after commit. Count actual fake-provider dispatches. Prove committed outputs reused and ambiguous calls never claimed exactly-once. Never reset counters through resume or worker restart.

## Required phase output
Write tracking/P03-REPORT.md with baseline/implementation SHA, actual files changed, intent, migrations, commands with cwd/start/end/exit, gate-specific assertions and evidence paths, failures/fixes and unresolved issues. Update tracking/STATE.json only from observed evidence. Use PASS, FAIL, BLOCKED or NOT_RUN. A reported PASS must correspond to the committed tested code; docs-only evidence stamps may name the prior tested implementation commit explicitly.

## Agent execution instruction
Read this phase, proposal, inspection notes and relevant strict-gate rows. Trace existing implementation before editing. Preserve unrelated work. Make the smallest complete change satisfying behavior. Run required checks; fix regressions and repeat affected checks. Do not advance with unmet dependency gates or weaken acceptance rules. If blocked externally, continue independent authorized work and preserve a restart note; leave final readiness blocked. No automatic merge.
