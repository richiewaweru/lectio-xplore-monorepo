# P00 — Login first and establish baseline
Required gates: G01 G02

## Implement
Login-first prompt; inspect AGENTS.md and working tree. Pin baseline and create isolated implementation branch. Record installed dependency versions, target URL, frontend/backend served commit, database and provider configuration identifiers without secret values. Open intended verification site in implementing browser, authenticate through supported flow, confirm an authorized page and harmless read. Keep the browser profile/context available. Attempt one fresh baseline Unit journey early with real provider and a dedicated test lesson; preserve IDs and failures. Run existing gates and classify baseline failures.

## Gate conditions
Authentication can be BLOCKED independently; continue read-only inventory and offline tasks, but mark unattended live readiness BLOCKED and do not claim final PASS. Historical red baseline is expected evidence, not P01 acceptance.

## Required phase output
Write tracking/P00-REPORT.md with baseline/implementation SHA, actual files changed, intent, migrations, commands with cwd/start/end/exit, gate-specific assertions and evidence paths, failures/fixes and unresolved issues. Update tracking/STATE.json only from observed evidence. Use PASS, FAIL, BLOCKED or NOT_RUN. A reported PASS must correspond to the committed tested code; docs-only evidence stamps may name the prior tested implementation commit explicitly.

## Agent execution instruction
Read this phase, proposal, inspection notes and relevant strict-gate rows. Trace existing implementation before editing. Preserve unrelated work. Make the smallest complete change satisfying behavior. Run required checks; fix regressions and repeat affected checks. Do not advance with unmet dependency gates or weaken acceptance rules. If blocked externally, continue independent authorized work and preserve a restart note; leave final readiness blocked. No automatic merge.
