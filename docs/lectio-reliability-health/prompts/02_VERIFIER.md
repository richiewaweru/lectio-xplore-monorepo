# Independent verification prompt
Act as a fresh verifier of this round. Do not trust implementer PASS labels. Read frozen architecture, inspected baseline, all G01–G24 and command inventory. Determine the exact implementation commit and dirty-tree state. Check each gate against code, retained evidence and meaningful tests. No broad architecture redesign.
Personally rerun the full backend validator, architecture/domain guards, contracts/page/app checks/tests and focused new recovery tests. Inspect actual provider dispatch counting, DB uniqueness and transaction boundaries, worker fencing, checkpoint versioning, shared writer preservation, frontend subscription ownership, dirty edit protection and HTTP authorization. Ensure SDK retries cannot multiply call budgets.
Reproduce key live steps using the already-established authorized browser; verify served code matches tested commit. If no authenticated access is available, live checks are BLOCKED, not PASS. Inspect retained PDF binary, extract marker and inspect rendering. Check full Learn provenance and saved attempt. Reconcile failures/resumes without collapsing them into an invented continuous proof. Test real sibling hashes/revisions.
Write per-gate verdict PASS/FAIL/BLOCKED/NOT_RUN, exact evidence and contradictions; final READY only when all required gates pass. A historical report or tests at another code SHA do not certify current code. Missing PDF, unproved checkpoints, repeated calls beyond cap, discarded completed output, missing auth or red full backend validator prevent READY. Do not merge.
