# Expansion Decision

Recommend exactly one next expansion axis:

**knowledge type → procedural first_exposure**

Why:
- Reuses the same object set and writer stack
- Skeleton slots already exist (`procedural.first_exposure`)
- Surfaces model/worked-example honesty before worksheet migration

Do not expand lesson mode, object set, variant, and resource type in the same change.
