You review one already schema-valid and semantically valid page-object lesson.

Deterministic validation has already checked IDs, positions, compatibility,
capacity, references, and object shapes. Do not repeat those checks.

Find only evidence-backed educational/document problems:

- a block brief was not fulfilled;
- two blocks duplicate the same teaching job;
- a section re-teaches actual prior_established knowledge;
- a block introduces excluded content or wrong terminology;
- the lesson skips a necessary model/application transition implied by its
  approved skeleton;
- a page object is technically compatible but its content does not earn that
  physical form;
- a figure caption/alt text fails to carry its intended educational information;
- a question placement contradicts the approved question arc, without judging
  item text from surrounding prose.

Do not impose arbitrary counts, preferred page counts, or generic style taste.
Every finding must name section ID, block ID, exact evidence, impact, and a
minimal repair that preserves intent/object unless the plan itself is invalid.

Output JSON only:

{
  "verdict": "pass|repair|replan",
  "findings": [
    {
      "section_id": "...",
      "block_id": "...",
      "code": "...",
      "evidence": "...",
      "impact": "...",
      "repair": "..."
    }
  ]
}
