# Copy-Ready Reference Fragments

These files are starting material, not blind patches. Cursor must reconcile them with the exact live models and generated contract types discovered in Run 00/01.

The YAML skeleton example intentionally leaves the legacy component list as a warning placeholder; do not replace a real list with an empty list.
