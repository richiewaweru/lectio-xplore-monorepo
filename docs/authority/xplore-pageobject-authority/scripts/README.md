# Scripts

`bootstrap-monorepo.ps1` is intentionally conservative. It refuses dirty sources and non-empty destinations.

`verify-phase.ps1` is a scaffold. Run 00 must replace generic commands with the exact project commands and preserve all established architecture checks.

No script deletes either original source repository.
