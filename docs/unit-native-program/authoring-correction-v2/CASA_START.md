# Casa starter prompt

Implement the attached Lectio Authoring Correction Pack v2 from start to end on richiewaweru/lectio-xplore-monorepo, branch feat/unit-print-learn. Reviewed baseline is 998e9f3; inspect current head and preserve later fixes and dirty work. Do not reset to the baseline.

Read repository instructions, this pack's README, ARCHITECTURE, SOURCE_MAP and acceptance policy, then the original Unit Print/Learn pack if available in the repository or attachment. This pack carries forward unresolved defects and defines the current authoring correction. Live verification is explicitly deferred. Do not use missing live access as a reason to leave feasible offline implementation unfinished.

Execute A00–A06 in dependency order. Before each phase, reread its proposal and gates, inspect the real code, and write a short execution plan. Implement, run the gates, save evidence, update tracking, and commit coherent changes. Continue autonomously without pausing for routine confirmation. Preserve working Print execution and both paths' shared teaching revision.

The target is package-owned authoring definitions consumed by a reusable engine. Reuse existing catalogue writer views, provider abstractions and Print repair machinery. Register capability instructions/schemas/modes/validators; do not create a parallel catalogue or an engine for every component. Keep Print/Learn native selection, rendering and assembly appropriately separate.

Finish all carried-forward corrections: arbitrary Learn answers; brief-as-final-content; first-content/keyword selection; fallback eligibility bypass; incomplete package ownership; Print fixed-table fallback; split prompt loading; missing-contract fallback; incomplete result/provenance/assembly wiring. Review upstream source/action ownership and normal production routes. A disconnected helper is not implementation.

Use strict acceptance. Mock only external boundaries, assert independently specified answers, demonstrate old-defect regressions, and test definition changes reaching actual requests. Do not weaken tests, disable promised capabilities to reduce scope, invent evidence or declare readiness from schemas alone. Assembly must consume validated results and fail on missing ones. No production fixture substitution is allowed.

Record exact commands, tested commit, output, gate IDs and evidence paths. Keep historical live evidence separate. If blocked, record the precise blocker and continue independent feasible work; dependent gates remain non-passing. Do not deploy, merge to main or modify production data.

At completion deliver phase commits, requirement-to-test mapping, changed behavior, remaining blockers and deferred live checklist. Say 'Offline corrective gates passed; live/model-quality verification deferred' only when all mandatory offline gates pass. Otherwise say 'Corrective implementation incomplete' and identify what remains. Do not stop at writing a plan or scaffolding.
