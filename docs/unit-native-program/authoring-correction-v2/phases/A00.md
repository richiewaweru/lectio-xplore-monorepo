# A00 — Baseline and defect regressions
Dependencies: None

## Proposal and implementation scope
Inspect AGENTS instructions, original pack, current branch/head, worktree and all source-map paths. Record later fixes before editing. Map normal Unit-to-Print/Learn entrypoints and actual prompt/provider calls. Discover repository commands rather than inventing them. Add targeted failing regression tests for arbitrary answers, brief-as-content, Print table fallback, first-choice ranking and fallback budget bypass. Isolate failures so one issue does not mask the others. Record old behavior honestly; already-fixed defects need evidence rather than forced failure.

## Acceptance gates
- **A00-G01**: Baseline records branch/head, dirty work, relevant instructions, commands and production call graph.
- **A00-G02**: Tests expose each reviewed defect or document an independently verified later fix; failures occur for the intended reason.
- **A00-G03**: Requirement-to-file/test mapping covers both paths, all core interactions and generation-enabled Learn content capabilities.

## Execution prompt
Implement A00 after reading README.md, ARCHITECTURE.md, SOURCE_MAP.md, acceptance/POLICY.md and this phase. Inspect the current implementation and required predecessors. Write a concrete phase plan in tracking/A00-PLAN.md before editing. Use tests that demonstrate the required behavior and expose the previous defect where applicable. Implement the scope above, run the phase gates, and record exact evidence in tracking/GATE_RESULTS.csv and tracking/A00-REPORT.md. Do not equate schema validity with meaningful content. Commit a coherent change set. Continue into the next unblocked phase without asking for routine approval. If a predecessor is blocked, do not mark dependent integration gates passed; continue only independently feasible work. Leave unresolved gates non-passing and explain the blocker.
