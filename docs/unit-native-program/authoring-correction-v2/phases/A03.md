# A03 — Migrate Print safely
Dependencies: A02

## Proposal and implementation scope
Route normal dispatch_writer_async through the package-loaded authoring mechanism. Consolidate active prompt construction; remove bypassed mappings and advisory missing-contract success. Keep exact approved assessment conversion. Remove broad exception-to-stub production fallbacks, especially fixed leaf tables. Figures retain explicit pending/failed asset stages and request identity; they must not pretend a failed brief is a completed asset. Preserve worker concurrency/checkpoints and targeted retry. Avoid rewriting pagination or PDF code for this task.

## Acceptance gates
- **A03-G01**: Normal Print entrypoint delivers selected package instructions and exact form schema to provider.
- **A03-G02**: Table provider failure yields typed failure/retry; no Lit leaf/Covered leaf or other fixture content is produced.
- **A03-G03**: Approved question/choice conversion preserves IDs, answer keys and student/teacher separation.
- **A03-G04**: Offline regressions cover prose, list, table, aside, worked example, questions, choices and figure lifecycle where selectable.
- **A03-G05**: Retrying one failed block preserves completed siblings; assembly/reload retains validated payloads and order.

## Execution prompt
Implement A03 after reading README.md, ARCHITECTURE.md, SOURCE_MAP.md, acceptance/POLICY.md and this phase. Inspect the current implementation and required predecessors. Write a concrete phase plan in tracking/A03-PLAN.md before editing. Use tests that demonstrate the required behavior and expose the previous defect where applicable. Implement the scope above, run the phase gates, and record exact evidence in tracking/GATE_RESULTS.csv and tracking/A03-REPORT.md. Do not equate schema validity with meaningful content. Commit a coherent change set. Continue into the next unblocked phase without asking for routine approval. If a predecessor is blocked, do not mark dependent integration gates passed; continue only independently feasible work. Leave unresolved gates non-passing and explain the blocker.
