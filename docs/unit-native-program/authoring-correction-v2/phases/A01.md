# A01 — Complete package authoring definitions
Dependencies: A00

## Proposal and implementation scope
Extend existing writer views with the architecture contract. Put authoring instructions beside owning package definitions; export instructions or stable resolvable references. Reconcile existing Print prompt files with package guidance. Include modes, input requirements and validators without duplicating schemas. Give every production-selectable capability a complete definition. Preserve native policy ownership and shared vocabulary. Regenerate consumers with existing tooling. Add definition hashing and readiness checks; absent instructions or wiring must prevent generation readiness.

## Acceptance gates
- **A01-G01**: Every generation-enabled capability resolves instructions, exact schema, required inputs and registered validation/mode support.
- **A01-G02**: A package-only instruction change reaches the actual writer request after regeneration without dispatcher edits.
- **A01-G03**: Missing resources and unknown validators fail explicitly; generated exports pass drift checks.
- **A01-G04**: Instruction/schema changes alter definition hash; old persisted releases are unchanged.

## Execution prompt
Implement A01 after reading README.md, ARCHITECTURE.md, SOURCE_MAP.md, acceptance/POLICY.md and this phase. Inspect the current implementation and required predecessors. Write a concrete phase plan in tracking/A01-PLAN.md before editing. Use tests that demonstrate the required behavior and expose the previous defect where applicable. Implement the scope above, run the phase gates, and record exact evidence in tracking/GATE_RESULTS.csv and tracking/A01-REPORT.md. Do not equate schema validity with meaningful content. Commit a coherent change set. Continue into the next unblocked phase without asking for routine approval. If a predecessor is blocked, do not mark dependent integration gates passed; continue only independently feasible work. Leave unresolved gates non-passing and explain the blocker.
