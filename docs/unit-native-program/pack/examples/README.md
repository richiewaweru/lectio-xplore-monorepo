# Examples
These are proposed contract examples, not existing application payloads. Translate through the implemented native schemas; never insert them to claim a live run. Runtime IDs are illustrative.
capability.schema.json validates the illustrative capability record only. Production schemas must be generated from implemented package sources, including exact kind-specific payloads and semantic validation.
cycle-shared-plan.json demonstrates shared meaning without component IDs. cycle-native-decisions.json demonstrates native choices after narrowing. The callback-free data model is intentional.
