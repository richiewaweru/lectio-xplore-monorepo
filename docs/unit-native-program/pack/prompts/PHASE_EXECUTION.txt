Execute phase {{PHASE_ID}} using its phase document and the master prompt. Confirm dependencies, inspect actual owners, list gate-to-test mappings, implement the smallest complete change, run tests and inspect outputs. Update the phase report and tracking files. Mark PASS only when every required gate has evidence. Preserve package ownership, context isolation, shared-plan identity, exact native contracts and existing edits. Continue to the next eligible phase after success. These placeholders are operator inputs, not application prompt syntax.
