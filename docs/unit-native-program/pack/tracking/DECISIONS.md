# Decision log — Unit Print/Learn program

Record implementation choices without rewriting pack intent.

| ID | Date | Question | Decision | Evidence / rationale | Affected contracts / gates |
|---|---|---|---|---|---|
| D-001 | 2026-09-07 | Where to host campaign tracking? | `docs/unit-native-program/` with pack copy under `pack/` | Pack master prompt proposes this location | P00 |
| D-002 | 2026-09-07 | FE-001 fix strategy? | Retarget imports to `$lib/print/...` / `$lib/learn/...` / `$lib/shared/...` owners; narrow domain-guard needle that wrongly banned `$lib/print/studio` | Matches CURRENT_SYSTEM and FE-001 acceptance; Vite `$lib` alias precedence blocked public `$lib/studio` remaps | P00-B01 / P00-B03 |
| D-003 | 2026-09-07 | QuizCheck role vs LEARN-009? | Remove conflicting `role=radio` on `<button>`; use button + `aria-pressed` for click-to-answer | Immediate-submit MCQ is not a radio group; restores accessible name for tests and correct UX | P00-B02 |
| D-004 | 2026-09-07 | Stale agents/project.md? | Rewrite to Unit-path curriculum/print/learn/infra ownership | Pack 02_SOURCE_REVIEW; CURRENT_SYSTEM is authoritative | P00-B03 |
| D-005 | 2026-09-08 | How should `@lectio/contracts` be consumed inside the monorepo? | Source-only package: `exports` point at `src/index.ts`, no build step | Keeps the neutral package free of a toolchain and of any Svelte/Python dependency; workspace consumers already transform TS | P01-K06 |
| D-006 | 2026-09-08 | Should Learn content capabilities be authored or generated? | Generated from the component registry and `export-policy.ts` | Section components already carry metadata, a Zod schema, field contracts and an export policy; a second hand-written catalogue would drift, and 01_CAPABILITY_CONTRACT requires generated indices | P01-K01 / P01-K02 |
| D-007 | 2026-09-08 | May a text interaction claim `generation-ready` because it has a schema and an evaluator? | No — `planned` / `incomplete` with a stated path until a native selection policy can choose it and a Builder editor can repair it | A UI shell with an evaluator is not a generation capability; readiness must describe what a consumer can actually run | P01-K05 |
| D-008 | 2026-09-08 | How to check "no native inventory in the teaching view" when native ids overlap ordinary English (`prose`, `table`, `numeric`)? | Exact key/value equality against every native id, plus free-text substring search restricted to identifier-shaped (hyphenated) ids; `classify`, `sequence`, `match-pairs` recorded as shared-by-design | Matching `numeric` inside "Give a numeric answer" is a false positive; hyphenated ids never occur in natural prose, so a hit there is a real leak | P01-K06 |
| D-009 | 2026-09-08 | `classify` with multiple categories per item? | Single-category membership only, declared in the record and enforced by the evaluator | The shared evaluator scores item→category as source→target pairs, so a second category for the same item is a duplicate source and is rejected; declaring multi-category support would be aspirational | P01-K03 / P01-K04 |
| D-010 | 2026-09-08 | Spatial interactions (`image-hotspot`, `drag-label`)? | Stay `unavailable`, with per-capability blocking reasons and a path to readiness | Pack constraint: spatial stays unavailable while authoring is incomplete. Regions cannot be produced or verified against a real asset today | P01-K05 |
| D-011 | 2026-09-08 | `short-response` contracts authored against the old numeric shape? | Reject rather than silently migrate; document the migration on the capability record | `{ value, tolerance }` never produced a defensible score for a written answer, and no committed content authored one, so there is nothing to backfill | P01-K03 |
| D-012 | 2026-09-08 | Where does the instructional teaching model live? | `curriculum/teaching_plan/`; Print re-exports and calls `plan_shared_teaching` | FILE_OWNERSHIP + TARGET_ARCHITECTURE; avoid a second planner | P02-S04 |
| D-013 | 2026-09-08 | How to drop print-native assumptions from Unit prep without a third StructuralPlan version? | Persist shared prep as `document_contract_version=1` with empty components + `shared_preparation=True`; do not set `native_whole_lesson` at prepare | Shared meaning must not masquerade as print v2; P03 owns realization identity | P02-S01 |
| D-014 | 2026-09-08 | Instance ids for non-repeated skeleton slots? | Bare role for singles (`orient`); `role-N` only when the role repeats | Preserves existing packet slot ids while guaranteeing uniqueness under repeats | P02-S03 |
| D-015 | 2026-09-08 | Where do Print/Learn realization identities live? | Additive `native_realizations` table with DB unique identity key | Chunked-state alone cannot enforce concurrent uniqueness; contracts/03 requires transaction-safe idempotency | P03-R01 / P03-R06 |
| D-016 | 2026-09-08 | Ambiguous legacy generations? | `status=read_only`, `variant_id=legacy-ambiguous`; never guess path | Prevents silent Print/Learn mis-routing and keeps the normal identity key free for regenerate | P03-R05 |
| D-017 | 2026-09-08 | Status before a native output artifact exists? | Realization row is authoritative; status may read shared preparation generation until `output_id` is set | Avoid inventing generation IDs that do not exist; open href stays null until output is linked | P03-R06 |
| D-018 | 2026-09-08 | May incomplete Learn interactions be selected? | Yes when native policy offers them and a writer schema exists; Builder/generation-ready remains P06 | P04-N01 requires Sequence selectable for order-items without claiming publish-ready | P04-N01 / D-007 |
| D-019 | 2026-09-08 | Gate wording `reconstruct-order` vs vocabulary `order-items`? | Selection accepts `reconstruct-order` as an alias of `order-items`; teaching vocabulary stays `order-items` | Avoids a second action id while matching Sequence's cognitive job language in gates | P04-N01 |
| D-020 | 2026-09-08 | Form planner after teaching approval on Unit native path? | Closed deterministic selection + work-order trace (`native_production`); LLM form planner remains for studio/legacy | P05 requires uninterrupted handoff without injected plans; P04 closed set is authoritative | P05-P01 |
| D-021 | 2026-09-08 | Can figure be selected before a raster exists? | Yes via deferred visual asset token for required/visual intents; export still blocks on pending/failed assets | Matches “tracked dependency, not blank success” without violating P04 empty-asset deny when assets are explicitly empty | P05-P03 |
| D-022 | 2026-09-08 | P05-P04 page-image inspection without Poppler? | Mark image inspection BLOCKED; keep PDF text/overflow assertions as PASS | Environment lacks `pdftoppm`; content integrity still evidenced | P05-P04 |
| D-023 | 2026-09-08 | When may Sequence claim `generation-ready`? | After native policy offer + deterministic writer/assemble + Builder interaction editor | Closes D-007/D-018 for Sequence only; spatial remains unavailable | P06-L01 / P06-L03 |
| D-024 | 2026-09-08 | Publish double-click idempotency? | Same draft `document_hash` returns the existing release (`idempotent_replay`) | Prevents duplicate immutable snapshots without a separate key store | P06-L05 |
| D-025 | 2026-09-08 | PathLesson provenance on unit-sourced drafts? | Always pin `LessonProvenanceModel` revision/objective_hash; never stamp live PathLesson onto an older draft | Matches 04_RUNTIME_AND_RELEASE | P06-L06 |
| D-026 | 2026-09-08 | Shared evaluator strategy for P07? | Python Sequence evaluator with golden parity to `@lectio/learn` TS; reject malformed responses; no client scores | Closes LRN-002 without a second hand-scored dialect | P07-U01 / P07-U02 |
| D-027 | 2026-09-08 | Default score aggregation across retries? | `latest`, overridable per contract (`first`/`best`); practice bucket separate | Prevents silent numerator/denominator inflation | P07-U05 |
| D-028 | 2026-09-08 | Class analytics instance scope? | Only instances with `assignment_id` targeting that class’s assignments and matching release | Fixes LRN-007 over-broad self-started inclusion | P07-U06 |
| D-029 | 2026-09-08 | Builder component_id for Sequence contracts? | Host on registered content component until `learn-interaction:*` is registered | Unblocks publish fixtures without claiming a new shell | P07 gates |
| D-030 | 2026-09-08 | Unit→Learn without D6B plan swap? | Closed Learn production from approved shared teaching (`native_production` / `native_execution`) | P08 requires uninterrupted chains; D6B retained as regression only | P08-I01 |
| D-031 | 2026-09-08 | How can curriculum façade call Print planner without importing print? | `bind_shared_teaching_runner` at Print composition root | Satisfies backend domain guards while keeping a single planner | P08-I05 |
| D-032 | 2026-09-08 | May deterministic P08 tests claim live readiness? | No — labelled mocks only; live campaign uses LIVE_READY_RUNBOOK + LIVE_PROTOCOL | Separates integration class from live class of proof | P08-I06 |
| D-033 | 2026-09-08 | May Unit `shared_preparation` enter native Print stage2 without print contract v2 at prepare? | Admit when path provenance is present; require contract only for pure Print prepare | Unlocks Unit→Print without forcing a Print-shaped prepare on shared teaching | P09-V01 |
| D-034 | 2026-09-08 | Teach practise/diagnose/check with unused approved assessment items? | `_repair_missing_assessment_sources` binds unused approved items (not orient/explain) | Avoids `NO_COMPATIBLE_CAPABILITY` when teaching omitted assessment_item_ids | P09 live Case A |
| D-035 | 2026-09-08 | Learn policy offered IDs vs real catalogue? | Use real content/action ids; Sequence-only action defaults; content fallbacks for empty sets | Fixes empty candidate / wrong-id selection failures | P09 live Learn |
| D-036 | 2026-09-08 | Is `native_learn` an active publish source? | Yes — add to `_ACTIVE_SOURCES` | Unit-native Learn drafts were 404ing on publish | P09-V03 |
| D-037 | 2026-09-08 | MC sources on intents that do not support `choices`? | Clear incompatible sources; bind only kind-matched forms from form-selection-view | Prevents `NO_COMPATIBLE_CAPABILITY` empty sets on practise-guided/apply | P09 live B/C |
| D-038 | 2026-09-08 | Model briefs omit owned terminology? | Deterministic brief grounding repair appends owned vocab tokens | Unblocks BRIEF_NO_ANCHOR_OR_TERM without inventing page-object ids | P09 live B |
| D-039 | 2026-09-08 | Live campaign on Print form failure after teaching approve? | Continue Learn on the same teaching revision; mark PASS_WITH_BLOCKERS | Dual-path Learn evidence must not be discarded when Print selection fails | P09-V03/V04 |
| D-040 | 2026-09-08 | Playwright PDF under Windows uvicorn? | Run print render on a dedicated Proactor loop via `asyncio.to_thread` | SelectorEventLoop cannot spawn Chromium (`NotImplementedError`) | P09-V02 |
| D-041 | 2026-09-08 | `@lectio/contracts` package entry for SSR? | Export built `dist/` (not raw `.ts`); Vite `ssr.noExternal` for workspace pkgs | Studio print SSR 500: Unknown file extension `.ts` | P09-V02 |
| D-042 | 2026-09-08 | Print page document API path? | Use `/api/v1/v3/generations/...` (not `/v3/print/generations/...`) | FE prefix lagged backend mount; document fetch was 404 | P09-V02 |
| D-043 | 2026-09-08 | Live teaching omits learner_action on order objectives? | Deterministic `_repair_missing_order_learner_actions` attaches `order-items` with empty `source_item_ids`; selection/work-orders must not fall back to MC assessment ids for order-items | Unlocks Sequence for attempt proof without inventing MC→Sequence bindings | P09-V03 |
| D-044 | 2026-09-08 | How to prove live Print recovery (P09-V05)? | Arm `XPLORE_NATIVE_FAILURE_*` for `planning_forms` once on exact generation id; recover via product `retry-native`; assert opposite-path Learn release `document_hash` unchanged | Uses existing test-only injection; no global provider disruption | P09-V05 |
| D-045 | 2026-09-09 | May selection invent learner actions or prefer Sequence for writer readiness? | No — omit missing actions; rank closed shortlists with package choose_when/reject_when; `select_learn_first_legal` is a test helper only | Supersedes D-035 Sequence defaults and Sequence-writer bias | P04 corrective |
| D-046 | 2026-09-09 | When may core interactions claim generation-ready? | Only after writer + assemble + renderer + evaluator + Builder edit schema are connected; then export selection view via isSelectable | Supersedes D-007/D-018/D-023 Sequence-only readiness for the core eight; spatial stays unavailable | P01-K05 / P06 / P07 |
| D-047 | 2026-09-09 | Missing order action or assessment ownership at teaching? | Structured teaching repair errors (`TEACHING_MISSING_ORDER_ACTION` / `TEACHING_MISSING_ASSESSMENT_OWNERSHIP`); bind assessments only on kind + concept/purpose overlap — never silent order-items or `pool.pop(0)` | Supersedes D-034/D-043 silent repairs; Learn must not invent Sequence | teaching ownership / P08 |
