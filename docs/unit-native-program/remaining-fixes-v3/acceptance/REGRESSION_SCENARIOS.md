# Explicit tests and counterexamples

1. Generated Numeric activity: brief 'Create a distance calculation using speed and time'; scoped facts v=5 m/s, t=10 s, d=v*t. Mock provider returns student question 'A cyclist travels at 5 m/s for 10 seconds. How far does she travel?', config value 50/unit m, feedback explaining multiplication. Assert question and feedback survive; 50 correct, 5 incorrect. The expected answer is independently authored, not copied from production output. Also test the exact request facts, because a fixed response mock alone cannot prove input completeness.
2. Approved Choice q2: stem 'What is evaporation?', correct key b. Include unrelated q1 containing sentinel UNRELATED_SOURCE_MUST_NOT_LEAK. Assert q2 stem/key retained and q1 absent from initial and repair payloads. A source-free explanation alongside q2 must remain generation mode.
3. A source-free work order with a nonempty global pool must not bind the first question. Explicit nonexistent q9 must fail before provider invocation. Two items claiming ID q2 must fail as ambiguous.
4. Sequence with IDs 'Stage_A', 'Stage_B' and item IDs matching exactly must preserve them, not slugify only order. Accepted-answer alternative arrays must retain canonical structure.
5. Required factual context empty -> MISSING_AUTHORING_INPUT or repository equivalent; no model call. Optional terminology=[] is valid. Complete conversion input may succeed without duplicated fact list if package definition permits it.
6. Two legal candidates with deliberately misleading lexical scores: mock selector returns the nonfirst lower-overlap candidate; assert it is used and call recorded. Return forbidden ID first, legal ID on repair; assert original shortlist is retained. Always-invalid provider must exhaust finite repair with typed error.
7. DB integration: seed legitimate user and shared preparation fixture, run actual native production routes, persist, close session, reload from fresh session. Do not use deepcopy as a reload substitute.
8. Release integration: API publish v1 -> API Builder edit -> API publish v2 -> fresh reads of both snapshots. Hash comparisons supplement these actions rather than replace them.
9. Runtime integration: submit learner answer with forged client score; server computes authoritative result. Reload attempt and verify stored outcome, release reference and concept/completion behavior.
10. Component integration: mount actual components with writer-produced contracts via mocked provider, interact with inputs, submit, assert response payloads. Label this offline rendering evidence, not a live browser journey.

Keep each test focused enough to fail for the specific defect. Do not combine all failures into one end-to-end test that hides later defects. Then add integrated tests connecting the fixed boundaries. Preserve canonical application validation and authorization layers; use test fixtures/dependency overrides only as repository practice permits.
