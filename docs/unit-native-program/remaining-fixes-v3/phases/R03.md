# R03 — Implement configured semantic selection
## Work
Implement R4 in Learn and relevant Print adapters. Preserve correct existing planner decisions. Add bounded selector repair, provider provenance and async integration.
## Mandatory gates
- R03-G01: Ambiguous Learn content and interaction decisions invoke configured selector through normal production.
- R03-G02: Ambiguous Print production selection invokes configured selector or consumes an already valid model-produced sealed decision without reselecting.
- R03-G03: Mock model selects a legal nonfirst lexically disfavored capability and production honors it; sole candidate avoids an unnecessary call.
- R03-G04: Invalid model ID/required-interaction omission triggers bounded repair; exhaustion returns typed failure with no keyword fallback.
- R03-G05: Candidate order permutation preserves the closed-set semantics and no production hardcoded component keyword bonuses remain.
## Execution instruction
Read SPECIFICATION.md and this phase before editing. Write a phase plan, implement, run meaningful tests, save evidence and update tracking/GATES.csv. Commit coherent changes. Continue to the next unblocked phase autonomously. If a predecessor is blocked, dependent gates cannot pass; continue only independent feasible tasks. Do not quietly redefine a gate. Report exact implementation or environment blocker and unresolved work.
