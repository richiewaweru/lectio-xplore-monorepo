# C5 PHASE PLAN — Guards + Integration Proof

1. Extend backend/frontend guards: reject `$lib/learn/shared` leftovers; keep shim exclusions for remaining shims; note deleted DEAD modules
2. Run: domain-guards, Alembic/ORM metadata, `@lectio/page` test+check, focused `@lectio/learn` tests, focused Builder/Learn pytest, focused frontend vitest
3. Document Unit→Print / Unit→Learn residual gaps (no silent skip)
4. Write final tree + residual list in PHASE_REPORT; update CLEANUP_STATE on PASS
