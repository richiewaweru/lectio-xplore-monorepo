# Search / Negative Gates

Search for residual v1 Learn:

```text
build_closed_learn_production
build_closed_learn_production_async
host_interaction_blocks_for_builder
closed_learn_selection
explanation-block
LessonDocument v1
```

Search tracking/docs:

```text
pending-commit
PASS with
deferred
```

Search interaction selector:

```text
interaction-selection
effective_prompt_text("interaction-selection")
```

Prove the prompt can affect a multi-candidate decision if documentation claims LLM selection.

Review action vocabulary references:

```text
select-one
select-many
complete-missing-values
classify-items
match-pairs
order-items
reconstruct-order
enter-number
enter-text
describe-in-own-words
```

Mutable mappings should remain policy-backed rather than duplicated in production code.
