# Expected End State

```text
Teaching Plan
├── learner_action guaranteed legal
├── no native path ids
└── approved once
       │
       ├───────────────┐
       ▼               ▼
     PRINT            LEARN
       │               │
 shared compose      shared compose
 shared write        shared write
       │               │
 YAML Print map      YAML Learn map
       │               │
 paper treatment     interaction
       │               │
 LectioDocument v2   LearnDocument v2
 native editor       editor/runtime
       │               │
      PDF             WEB
```

No active v1 ordinary Learn architecture remains.

Prompt/policy claims match runtime behavior.

The exact user entrypoints and final artifacts are proven.
