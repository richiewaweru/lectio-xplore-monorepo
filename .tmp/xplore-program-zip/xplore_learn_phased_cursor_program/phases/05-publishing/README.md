# Phase 05 — Student Preview + Explicit Immutable Publishing

    ## Goal
    Preserve the existing generate→edit flow, add true student preview, and create immutable published LearnRelease snapshots.

    ## Prerequisite
    All previous program phases, especially phase 04, must have a PASS report.

    ## Expected outputs
    - phase report
- LearnRelease schema/contract
- publish state-machine notes

    Run this phase by pointing Cursor to `CURSOR_PHASE_PROMPT.md`.
