# Phase 06 — Runtime Persistence + LearningInstance Execution

    ## Goal
    Create the minimum persistent learner runtime that can execute a published release, record immutable attempts, resume, and complete lessons.

    ## Prerequisite
    All previous program phases, especially phase 05, must have a PASS report.

    ## Expected outputs
    - phase report
- runtime schema map
- runtime API contract
- rebuild/projection test evidence

    Run this phase by pointing Cursor to `CURSOR_PHASE_PROMPT.md`.
