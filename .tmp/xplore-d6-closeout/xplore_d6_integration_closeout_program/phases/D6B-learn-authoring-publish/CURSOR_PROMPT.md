# D6B — Unit → Learn → Builder → Publish

Read all permanent D6 documents first.

## Objective

Build/strengthen integration:
Unit → Learn generation → Builder → edit → save → reload → Preview → Publish v1 → edit draft → Publish v2.

Assert document validity, real persistence, preview non-mutation, and v1 immutability after v2.


## Protocol
1. Record branch/SHA/dirty state.
2. Inspect existing production services/tests.
3. Write a short plan.
4. Implement only this subphase.
5. Run relevant tests.
6. Record every failure and classification.
7. Write `docs/d6/reports/D6B/PHASE_REPORT.md`.
8. Update `docs/d6/D6_STATE.md` only on PASS.
9. STOP.
