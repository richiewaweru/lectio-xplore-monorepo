You are executing the Xplore canonicalization + cleanup program in `C:\Projects\lectio`.

Begin with **C0 only**.

Before every phase:
1. reread all `permanent/` documents,
2. reread that phase's `CURSOR_PROMPT.md`,
3. read `docs/cleanup-program/CLEANUP_STATE.md`,
4. read `docs/cleanup-program/REACHABILITY_MANIFEST.json`,
5. read the previous phase report.

The Unit path is the only supported lesson creation path.

This program is for canonicalization, separation, safe splitting/duplication, dead-code deletion, repo/doc cleanup, and integration proof. Do not implement deferred runtime/security/analytics/product fixes.

For each phase: inspect → plan → implement → verify → report → update state → stop. Proceed only on PASS.
