# Xplore Canonicalization + Dead-Code Cleanup Program

Second-stage refactor after domain relocation.

Goal: make the Unit path the only supported lesson-creation path, complete Print/Learn separation, remove stale/legacy code and docs, and prove the remaining system still works.

Run sequentially: C0 → C1 → C2 → C3 → C4 → C5.

This is structural cleanup only. Runtime/security/analytics product fixes remain deferred.
