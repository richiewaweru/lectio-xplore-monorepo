Execute the Xplore D0–D5 legacy-retirement program in C:\Projects\lectio.

Start with D0 only.

Before every phase reread permanent docs, current phase prompt, LEGACY_STATE, dependency manifest, previous report.

The Unit path is the only supported lesson creation path.

Goal: map remaining legacy dependencies, migrate consumers, retire routes/jobs/telemetry/config, handle DB safely, then uproot unsupported legacy trees.

Do not implement deferred product fixes.

For each phase:
inspect → plan → implement → verify → report → update state → stop.
Proceed only on PASS.
