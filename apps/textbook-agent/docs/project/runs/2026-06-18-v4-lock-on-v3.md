# V4 Lock On V3

Objective: Implement the `v4-lock` goal on top of the restored `v3` branch in phase order, using the goal docs as the source of truth.

## Tracking Checklist
- [x] Re-read `lectio-v4-codex-goal.md`.
- [x] Re-read `lectio-v4-lock-proposal.md`.
- [x] Re-read `lectio-v4-prompt-and-reasoning-spec.md`.
- [x] Confirm work is on branch `v3`.
- [x] Phase 1.1 Cut dead pack-generation surface (`pack_runner.py` and `/api/v1/packs/generate`).
- [x] Phase 1.2 Remove stale architecture-test rules for `planning` and `pipeline`.
- [x] Phase 1.3 Rename `pipeline.*` logger namespace to `lectio.*`.
- [x] Phase 1.4 Replace stale `Kira Learning` ownership labels with `Lectio`.
- [x] Phase 1.5 Park `introduce` / `practice` pack specs after retiring the live pack-planning path.
- [x] Validate the completed Phase 1 changes before moving on.

## Phase 2 - Slim `v3_review` To Deterministic Checks Only
- [x] 2.1 Keep deterministic checks in `backend/src/v3_review/deterministic_checks.py`.
- [x] 2.2 Remove LLM review from `backend/src/v3_review/reviewer.py`.
- [x] 2.3 Remove repair-target building from `backend/src/v3_review/reviewer.py`.
- [x] 2.4 Delete `backend/src/v3_review/llm_review.py`.
- [x] 2.5 Delete `backend/src/v3_review/repair_router.py`.
- [x] 2.6 Delete `backend/src/v3_review/targets.py`.
- [x] 2.7 Update `backend/src/v3_review/__init__.py` exports.
- [x] 2.8 Rewire `backend/src/v3_execution/runtime/runner.py` to deterministic-only review with visible flags.
- [x] 2.9 Update `backend/tests/v3_execution/test_v3_execution_core.py` for the deterministic-only path.
- [x] 2.10 Update `backend/tests/v3_review/*` for removed LLM/repair symbols.
- [x] Verify Phase 2 with targeted and full backend validation.

## Phase 3 - Remove The Standard Architect Path
- [x] 3.1 Locate and review the remaining standard-path implementation used by the V3 studio blueprint flow.
- [x] 3.2 Harvest any clean shared helpers worth preserving.
- [x] 3.3 Remove standard-path backend branching from `generation/v3_studio/router.py` and related modules.
- [x] 3.4 Remove standard-path frontend branching from `frontend/src/routes/studio/+page.svelte`.
- [x] 3.5 Update tests for the chunked-only path.
- [x] Verify Phase 3 with backend and frontend validation.

## Phase 4 - Delete `curriculum_enrichment`
- [x] 4.1 Confirm the chunked path has no live `curriculum_enrichment` consumer.
- [x] 4.2 Delete `backend/src/curriculum_enrichment/`.
- [x] 4.3 Clean `backend/scripts/phase5_smoke.py`.
- [x] Verify Phase 4 with grep and backend validation.

## Phase 5 - Remove Lens / Clarification Over-Machinery
- [x] 5.1 Remove lens-selection machinery from `backend/src/generation/v3_studio/prompts.py`.
- [x] 5.2 Remove clarification round-trip from V3 studio backend DTO/router flow.
- [x] 5.3 Remove clarifying-stage frontend flow from `frontend/src/routes/studio/+page.svelte`.
- [x] 5.4 Remove dead clarification/lens types from `frontend/src/lib/types/v3.ts`.
- [x] 5.5 Decide whether to remove or fully inert `applied_lenses` fields in blueprint models.
- [x] Verify Phase 5 with backend/frontend grep and validation.

## Phase 6 - Lighten Stage 1 While Preserving Contract
- [x] 6.1 Confirm `backend/src/v3_blueprint/planning/structural_planner.py` is the live chunked Stage 1 prompt.
- [x] 6.2 Replace the heavy reasoning chain with the lean prompt contract from the docs.
- [x] 6.3 Preserve `StructuralPlan` / `SectionPlan` output shape, including `transition_note`.
- [x] 6.4 Enforce spec-vocabulary role output with a post-generation assertion.
- [x] 6.5 Confirm Stage 2 still consumes `transition_note` unchanged.
- [ ] Verify Phase 6 with plan-shape checks, backend tests, and runtime timing notes.

## Phase 7 - Manifest And Final Sweep
- [x] 7.1 Remove obsolete packages from `backend/pyproject.toml`.
- [x] 7.2 Run orphan-reference grep sweep for all cut symbols.
- [x] 7.3 Run full backend and frontend validation.
- [ ] 7.4 Run one real chunked generation/manual verification if environment permits.
- [ ] Record final status against the docs.

## Notes
- Confirmed: `learning_router` is pack-only.
- Confirmed: `learning.pack_runner.start_pack()` is a dead stub that raises a RuntimeError.
- Resolved for Phase 1.5: retired the live pack-planning entrypoint and startup pack-spec loading, then parked `introduce.yaml` and `practice.yaml` under `backend/resources/_parked/pack_specs/`.
- Validation completed:
  - `uv run pytest` in `backend` -> 293 passed
  - `python -m pytest tools/agent/tests/test_check_architecture.py` -> 3 passed
  - backend import smoke for `app`, `learning.routes`, and `core.llm.logging` passed
- Phase 2 discovery:
  - `backend/src/v3_review/reviewer.py` still imports `run_llm_review` and `build_repair_targets`.
  - `backend/src/v3_review/__init__.py` still exports `route_repairs`.
  - `backend/src/v3_execution/runtime/runner.py` still calls both `run_coherence_review` and `route_repairs` inside the coherence pipeline block.
  - `backend/tests/v3_execution/test_v3_execution_core.py` and `backend/tests/v3_review/test_v3_review_deterministic.py` still assume the repair path exists.
- Phase 2 completed:
  - `v3_review` now runs deterministic checks only and emits a coherence report without LLM or repair routing.
  - Deleted `backend/src/v3_review/llm_review.py`, `repair_router.py`, and `targets.py`.
  - `v3_execution.runtime.runner` now consumes the deterministic report directly and records zeroed repair metrics for trace compatibility.
  - Removed repair-router coverage and target-builder coverage from the V3 review/execution tests.
- Phase 2 validation:
  - `uv run pytest tests/v3_execution/test_v3_execution_core.py tests/v3_review/test_v3_review_deterministic.py` -> 12 passed
  - `uv run pytest` in `backend` -> 284 passed, 1 warning
  - `python -m pytest tools/agent/tests/test_check_architecture.py` -> 3 passed
  - backend import smoke for `v3_review` and `v3_execution.runtime.runner` passed
- Phase 3 completed:
  - Removed the standard V3 blueprint-generation endpoint and `architectMode` branch so studio runs chunked-only.
  - Deleted the architect-mode frontend toggle and removed `generateBlueprint` from the live UI/API path.
  - Updated chunked studio backend/frontend tests to reflect the single path.
- Phase 4 completed:
  - Confirmed `curriculum_enrichment` had no chunked-path consumer after Phase 3.
  - Deleted `backend/src/curriculum_enrichment/`.
  - Removed the stale enrichment branch from `backend/scripts/phase5_smoke.py`.
- Phase 5 completed:
  - Removed the V3 studio clarification round-trip and the frontend clarifying stage.
  - Removed lens-selection and `applied_lenses` from the V3 studio, Stage 1 plan models, blueprint models, preview DTOs, planning artifacts, and compile-order support adaptation path.
  - Deleted the orphaned `generation/v3_lenses` package after the live path stopped referencing it.
  - Renamed the subtopic-narrowing node/timeout labels away from `clarify` to keep the final sweep aligned with the docs.
- Phase 6 completed except timing proof:
  - Replaced the heavy Stage 1 reasoning chain in `backend/src/v3_blueprint/planning/structural_planner.py` with the leaner doc-aligned prompt.
  - Preserved `transition_note` and the Stage 2 continuity flow.
  - Removed the hardcoded role `Literal` and added resource-spec role validation in `validate_structural_plan(...)`.
- Phase 7 validation:
  - Orphan sweep: `rg -n "applied_lenses|LensEffect|AppliedLens|V3AppliedLens|missing_signals|clarif|generation.v3_lenses|architect_mode|curriculum_enrichment|PLANNING_ENRICHMENT_CALLER|SectionPlanEnrichment" backend/src frontend/src backend/tests frontend/src/lib backend/scripts tools` -> only intended `lesson_architect_model_settings` / non-cut terminology remained.
  - Targeted backend validation after Phases 5-6:
    - `uv run pytest tests/generation/test_v3_chunked_lifecycle.py tests/generation/test_v3_studio_prompts.py tests/generation/test_planning_artifact.py tests/v3_blueprint/planning/test_validators.py tests/v3_blueprint/planning/test_section_expander.py tests/v3_blueprint/planning/test_stage1_error_logging.py tests/v3_blueprint/test_phase1_foundation.py` -> 36 passed, 1 warning
  - Targeted frontend validation after Phases 5-6:
    - `npx vitest run src/lib/studio/v3-canvas.test.ts src/routes/studio/page.test.ts src/lib/api/v3.test.ts src/lib/stores/v3-studio.svelte.test.ts` -> 4 files passed, 28 tests passed
  - Full validation:
    - `uv run pytest` in `backend` -> 268 passed, 1 warning
    - `python -m pytest tools/agent/tests/test_check_architecture.py` -> 3 passed
    - `npx vitest run` in `frontend` -> 51 files passed, 170 tests passed

## Remaining
- Phase 6 timing proof is still open because this run did not capture a trustworthy real-generation before/after comparison on `v3`.
- Phase 7.4 real chunked generation/manual verification is still open pending environment readiness for an actual end-to-end run.
- Once those two proof points are captured, record final status against the docs and push.
